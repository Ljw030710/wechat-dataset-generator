# 复制为项目根目录的 .env，再填写需要使用的一组配置。
# 日常命令无需重复指定 --provider；命令行参数和终端环境变量优先。
LLM_PROVIDER=deepseek

# 方式一：DeepSeek
DEEPSEEK_API_KEY=
DEEPSEEK_MODEL=deepseek-chat

# 方式二：CLIProxyAPI，启用时将上面的 LLM_PROVIDER 改为 cliproxy。
# 未填写代理密钥时，仍尝试读取本机 CLIProxyAPI 配置。
# CLIPROXY_API_KEY=
# CLIPROXY_MODEL=代理实际提供的模型ID

# 方式三：其他兼容服务，启用时将上面的 LLM_PROVIDER 改为 custom。
# LLM_API_KEY=
# LLM_BASE_URL=https://your-provider.example/v1
# LLM_MODEL=your-model-id
